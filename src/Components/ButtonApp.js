import * as React from 'react';
import Button from '@mui/material/Button';

function ButtonApp(){
    return (
        <div>
            <Button variant='contained'>Hello Worold</Button>
        </div>
    );
}

export default ButtonApp;